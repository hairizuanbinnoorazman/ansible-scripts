[Unit]
Description=Hadoop Filesystem
Requires=network.target
After=network.target

[Service]
User=hadoop
Group=hadoop
ExecStart=/opt/{{ hadoop_version }}/sbin/start-yarn.sh
ExecStop=/opt/{{ hadoop_version }}/sbin/stop-yarn.sh

[Install]
WantedBy=default.target