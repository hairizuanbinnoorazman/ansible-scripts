[Unit]
Description=Hadoop Filesystem
Requires=network.target
After=network.target

[Service]
User=hadoop
Group=hadoop
ExecStart=/opt/{{ hadoop_version }}/sbin/start-dfs.sh
ExecStop=/opt/{{ hadoop_version }}/sbin/stop-dfs.sh

[Install]
WantedBy=default.target