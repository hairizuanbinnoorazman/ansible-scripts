{
  "data_dir": "/tmp/consul",
  "server": true,
  "bootstrap_expect": {{ groups['consul'] | length }},
  "log_level": "info",
  "ui": true,
  "client_addr": "0.0.0.0",
  "retry_join": ["{{ groups['consul'] | join('","') }}"]
}